# FinConfigCalc

FinConfigCalc 是一款面向管理报表加工场景的本地数据处理工具。它把原本散落在 Excel
公式、辅助列、VLOOKUP、Power Query 和临时脚本里的加工步骤，整理成一张可重复执行的
Excel 调度表，用于完成数据读取、清洗、映射、转换、校验、对账、分摊和报表输出。

它尤其适合这样的财务工作：每月都要从多个系统导出明细，按照固定口径反复整理，最终
形成损益分析、费用分析、管理口径明细或其他内部管理报表。

## 为什么需要这个工具

手工加工管报的难点通常不在某一个公式，而在整条加工链：文件多、步骤多、口径多，且
每月重复。一个单元格引用错误、一段公式没有拉到底、一次筛选没有清除，都可能让结果
偏离预期。人员交接时，真正的加工逻辑还常常只存在于某个人的操作习惯里。

FinConfigCalc 希望解决的不是“替财务做判断”，而是把已经明确的加工规则稳定执行：

- **减少重复操作**：一次配置读取、筛选、映射、汇总和输出步骤，以后按相同顺序重复运行。
- **沉淀管理口径**：将科目归类、组织映射、字段标准化和分摊因子放进配置文件，而不是藏在
	临时公式或个人电脑上的脚本中。
- **降低手工差错**：调度执行前检查必填列、步骤顺序和参数格式；某一步失败后立即停止，避免
	带错继续加工。
- **让过程更容易复核**：运行界面展示每一步的状态、耗时、输入输出行数和结果路径，日志可一键
	复制，便于定位是哪一步、哪份配置出现问题。
- **提高月结复用效率**：期间、场景、文件路径或规则变化时，调整参数即可复用既有流程，不必
	从头重做整套工作簿。
- **数据留在本机**：工具可离线运行，业务数据无需上传到外部服务。

> FinConfigCalc 更像一条由财务人员配置的“管报加工流水线”，不是新的财务核算系统，也不会
> 自动决定业务口径。规则是否合理、源数据是否完整以及最终报表是否符合管理要求，仍需要业务
> 人员确认。

## 适合哪些工作

| 日常任务 | 可以怎样使用 FinConfigCalc |
| --- | --- |
| 合并多期间、多主体明细 | 批量纵向拼接 Excel 或 Parquet，字段不一致时按字段并集对齐，并可增加来源标识 |
| 统一不同系统的字段 | 选择、重命名和排序字段，补齐缺失列，将指定字段转换为字符串或数值类型 |
| 维护管理口径映射 | 按配置表执行科目、组织、产品线等规则映射，并通过优先级控制命中顺序 |
| 整理报表数据 | 按条件筛选、补充固定字段、将月份宽表转成长表、按维度分组汇总 |
| 补充维度属性 | 根据维度元数据补充成员名称、上级编码等属性，并检查业务数据中的成员是否合法 |
| 做数据对账 | 比较两份数据的行级差异，或按期间、主体、科目等维度汇总后比较金额差异 |
| 执行费用分摊 | 根据分出维度、分入维度和权重因子分摊金额，可筛选因子并输出展开后的因子供复核 |
| 生成交付文件 | 输出 Excel 供业务复核或交付，也可输出 Parquet 作为后续步骤的中间数据 |

当前可编排的处理功能包括：

- `allocation`：按因子分摊；
- `assign_columns`：增加或覆盖场景、版本、来源等固定字段；
- `check_metadata`：检查维度元数据及业务数据中的成员合法性；
- `concat`：纵向拼接多个文件；
- `append_with_overwrite`：覆盖指定存量分区后追加新数据；
- `diff_by_row`、`diff_by_row_to_excel`、`diff_by_group`：行级或汇总级差异检查；
- `filter_by_conditions`、`filter_group_sum_to_excel`：条件筛选、分组汇总及 Excel 输出；
- `mapping_1t`：配置驱动的单表规则映射；
- `merge_dims_attribute`：关联并补充维度属性；
- `standardize_columns`：统一字段名称、类型和顺序；
- `unpivot`：将月份或指标宽表转换为长表。

## 一次典型的月度加工

例如，财务人员需要将多个主体导出的费用明细加工成产品线损益数据，可以把流程配置为：

```text
合并各主体明细
		-> 统一字段名和数据类型
		-> 筛选本期实际数据
		-> 映射管理科目与产品线
		-> 检查维度成员
		-> 按配置因子分摊公共费用
		-> 按管理维度汇总
		-> 与上游总额对账
		-> 输出 Excel 报表
```

首次使用时需要梳理并验证规则；以后月份通常只需更新源文件、期间或配置，再执行同一张调度表。
相比保存一份层层嵌套公式的“大 Excel”，每一步的目的、顺序和输入输出会更明确。

## 如何使用

### 1. 打开工具

收到打包版后，请保留完整的 `FinConfigCalc` 文件夹，双击其中的程序：

```text
FinConfigCalc\FinConfigCalc.exe
```

不能只复制 EXE。程序使用终端界面；安装了 Windows Terminal 时会自动使用它，以改善中文和
等宽字体显示。工具本身不要求联网。

从源码运行时，在仓库根目录执行：

```powershell
.\.venv\python.exe -m fin_config_calc.ui
```

### 2. 准备调度表

调度表是一个 `.xlsx` 或 `.xlsm` 文件，默认工作表名称为“调度”。每一行代表一个处理步骤：

| 列名 | 是否必填 | 作用 |
| --- | --- | --- |
| `order` | 是 | 唯一数值，决定步骤执行顺序，例如 `10`、`20`、`30` |
| `function` | 是 | 要执行的处理功能，例如 `service.standardize_columns` |
| `params` | 是 | JSON 对象，填写该步骤的输入文件、规则文件、输出文件及其他参数 |
| `enabled` | 是 | `Y` 表示执行，`N` 或空值表示跳过 |
| `comments` | 否 | 用业务语言说明步骤目的，会显示在预览和运行日志中 |

示例：

```text
order:    10
function: service.filter_by_conditions
params:   {"input_file_path":"D:/finance/detail.xlsx","input_sheet_name":"明细","conditions":{"period":"202601"},"output_file_path":"D:/finance/output/filtered.parquet"}
enabled:  Y
comments: 筛选 2026 年 1 月实际明细
```

在 JSON 参数中，Windows 路径建议使用 `/`，例如 `D:/finance/detail.xlsx`，以避免反斜杠转义问题。

### 3. 先预览，再执行

1. 在“调度表路径”中输入 Excel 文件的完整路径。
2. 确认“调度表名称”，未修改时默认为“调度”。
3. 点击“预览任务步骤”，检查启用的功能、顺序和备注。
4. 关闭正在打开的同名输出 Excel，再点击“执行调度”。
5. 根据进度和日志检查每一步的 `START`、`DONE`、处理行数及输出路径。
6. 执行完成后复核关键汇总数、差异结果和最终报表。

界面中的“使用说明”会打开随程序提供的完整用户手册，其中包含全部功能的参数、示例和常见
问题；源码中的同一份手册位于 [src/fin_config_calc/ui/user_guide.html](src/fin_config_calc/ui/user_guide.html)。

## 使用时要特别注意

- **先用小样本验证**：新映射、新分摊因子或新数据源上线前，先用少量脱敏数据核对结果。
- **保留勾稽检查**：对关键金额设置汇总对账步骤，并在交付前核对源数据总额、加工后总额和差异。
- **关闭输出文件**：Excel 正在占用同名文件时，程序通常无法写入。
- **失败后检查已生成文件**：任一步骤失败都会中止后续步骤，但不会自动删除此前已经输出的文件。
- **谨慎使用原位更新**：`append_with_overwrite` 会直接更新存量文件，首次使用或变更条件前应备份。
- **妥善维护配置**：调度表、映射表、元数据和分摊因子共同构成管理口径，建议按期间或版本归档。
- **反馈前先脱敏**：日志、配置和样本中可能包含路径或业务信息，对外发送前请移除敏感数据。

当前版本采用本地文件和线性步骤调度，不包含参数审批、权限控制、多人并发、集中式任务平台、
自动回滚或完整的数据版本管理。输入支持 `.xlsx`、`.xlsm` 和 `.parquet`，输出支持 `.xlsx` 和
`.parquet`。

## 技术框架

FinConfigCalc 使用 Python 3.12 开发，核心组件如下：

| 组件 | 用途 |
| --- | --- |
| Polars | 内存中的表格数据清洗、关联、汇总、转换和计算 |
| openpyxl | 读取和生成 Excel 工作簿及差异报表 |
| Textual / Rich | 提供本地终端界面、进度和日志展示 |
| Loguru | 记录各处理步骤的运行信息 |
| PyInstaller | 构建可分发的 Windows 目录版程序 |
| pytest / Ruff / Pyright | 测试、代码检查和类型检查 |

项目采用 `src` 布局和单向依赖：

```text
ui -> service -> utils
```

- `src/fin_config_calc/ui`：调度界面和内置用户手册；
- `src/fin_config_calc/service`：面向财务场景的处理步骤；
- `src/fin_config_calc/utils`：通用 DataFrame、文件读写和调度执行工具；
- `tests/unit`：底层工具与单项服务测试；
- `tests/integration`：从输入文件到输出结果的集成测试；
- `templates`：预留的参数模板目录。

## 本地开发

本仓库使用位于 `.venv` 的 Conda 风格 Python 环境。在 Windows 上，解释器位于
`.venv\python.exe`，不是 `.venv\Scripts\python.exe`。

安装项目及开发依赖：

```powershell
.\.venv\python.exe -m pip install -e ".[dev]"
```

运行测试和质量检查：

```powershell
.\.venv\python.exe -m pytest
.\.venv\python.exe -m ruff check .
.\.venv\python.exe -m ruff format --check .
.\.venv\python.exe -m pyright
```

项目编码约定见 [docs/coding-standards.md](docs/coding-standards.md)。

## 构建 Windows 程序

安装项目及打包依赖：

```powershell
.\.venv\python.exe -m pip install -e ".[build-exe]"
```

从仓库根目录构建：

```powershell
.\.venv\python.exe -m PyInstaller --clean --noconfirm fin-config-calc.spec
```

构建结果位于：

```text
dist\FinConfigCalc\FinConfigCalc.exe
```

Textual 运行在终端中，因此不能使用 PyInstaller 的 `--windowed` 模式。分发时必须复制整个
`dist\FinConfigCalc` 目录；目录版也更适合 Polars 等包含原生依赖的组件。

## 还需要补充的业务信息

为了让这份说明更贴近实际推广和交接，建议后续补充以下内容：

1. 一个经过脱敏的真实管报案例，包括原始文件、配置表、加工步骤和最终结果截图。
2. 当前主要服务的报表类型和使用部门，例如损益、费用、预算、经营分析或合并管理报表。
3. 实际可量化的改善数据，例如月度加工耗时、涉及文件数、数据量以及上线前后的差错情况。
4. 程序包的获取位置、版本发布方式、负责人和问题反馈渠道。
5. 团队认可的配置归档、复核、审批和备份要求。

这些信息不影响工具运行，但会让新用户更快判断它是否适合自己的工作，也能让“节省了多少时间、
降低了什么风险”从原则性描述变成可信的实际案例。
